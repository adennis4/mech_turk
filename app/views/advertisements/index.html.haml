.hero-unit
  %h1#welcome-title The Psychology of Advertising
  %h3 Welcome to the Advertisement Index Page
  %p#psych-image
    =image_tag "psychology_logo.png"

%p.thumbnails
  -@advertisements.each do |advertisement|
    =link_to(image_tag(advertisement.image_url), advertisement_path(advertisement))
